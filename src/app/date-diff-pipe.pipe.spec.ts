import { DateDiffPipePipe } from './date-diff-pipe.pipe';

describe('DateDiffPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateDiffPipePipe();
    expect(pipe).toBeTruthy();
  });
});
